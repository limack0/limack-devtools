# local state produced by the tools at runtime
.devbox-init/
*.bundle
.sneakersync-manifest

# tunnel credentials — secret, never commit
deploy/cloudflared/creds.json

# editor / OS noise
.DS_Store
*.swp
